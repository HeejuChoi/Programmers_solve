function solution(n) {
    let answer = [];
    for (let i = 1; i<=n; i++) {
        // 홀수
        if (i%2==1) 
            answer.push(i)
    }
    return answer;
}

// arr.push(i)
// arr에 i를 넣는다. 

